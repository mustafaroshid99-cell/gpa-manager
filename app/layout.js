export const metadata = {
  title: "GPA Manager",
  description: "Teacher GPA App",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>
        {children}
      </body>
    </html>
  );
}
